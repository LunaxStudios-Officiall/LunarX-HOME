# Accessibility changes

The rebuild removes icon-only primary navigation labels, adds accessible names to icon buttons, keeps form labels adjacent to controls, exposes loading/error states through live regions, preserves keyboard access to dialogs and adds reduced-motion handling. Touch targets and mobile navigation are enlarged without removing desktop shortcuts.
