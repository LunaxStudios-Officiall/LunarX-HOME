# Contributing

Run unit tests, release validation, and installer dry-run before proposing a change. Do not commit credentials, user data, generated state, application binaries, virtual disks, caches, or unverified catalog entries. Changes to privileged helper actions require a threat-model update and negative tests.

Public contributions cannot be accepted until the project owner selects a project license.
